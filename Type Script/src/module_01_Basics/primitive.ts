let firstName: string = "Riyazul"
let lastName: string = "Arfaat"
var age: number = 25
const title: string = "Mr."

let isMarried: boolean = false

let status = isMarried ? "Married" : "yet to marry"

let fullName: string = `${title} ${firstName} ${lastName} is ${age} years old, but ${status}.`
console.log(fullName)
